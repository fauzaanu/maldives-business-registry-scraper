{
  "title": "Maldives Business Registry Scraper",
  "description": "Input schema for scraping business details from Maldives Business Registry",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "queries": {
      "title": "Business Search Queries",
      "type": "array",
      "description": "List of business names or keywords to search for in the Maldives Business Registry",
      "editor": "stringList",
      "placeholderValue": "Enter business name or keyword",
      "minItems": 1,
      "prefill": ["investment", "capital", "mart"]
    },
    "maxRequestsPerCrawl": {
      "title": "Max Requests Per Crawl",
      "type": "integer",
      "description": "Maximum number of requests to process (leave empty for unlimited)",
      "minimum": 1,
      "default": 1000
    }
  },
  "required": ["queries"]
}